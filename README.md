# jobsub.app

Public site for JobSub. Static files on GitHub Pages.

## Company list

Edit `companies.json` — a JSON array of display names. Keep it alphabetical.
The homepage reads that file; the same names are also listed in `index.html` so the page still works if JavaScript is off.

```json
["Affirm", "Asana", "Stripe"]
```
